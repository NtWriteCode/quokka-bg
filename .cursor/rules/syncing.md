---
trigger: model_decision
description: Only apply if the prompt is about a feature or code change that should be synced to a server in order to restore on another device.
---

Implement a way in the sync model to also load/save this extra information. 